<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">RefreshDataAsyncSample</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="webapiadaptor">
                <span class="oi oi-plus" aria-hidden="true"></span> WebApiAdaptor
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="urladaptor">
                <span class="oi oi-list-rich" aria-hidden="true"></span> UrlAdaptor
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="odataadaptor">
                <span class="oi oi-list-rich" aria-hidden="true"></span> ODataAdaptor
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
